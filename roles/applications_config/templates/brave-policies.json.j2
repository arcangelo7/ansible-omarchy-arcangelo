{
  "ExtensionInstallForcelist": [
{% for extension in brave_extensions %}
    "{{ extension.id }};https://clients2.google.com/service/update2/crx"{{ "," if not loop.last else "" }}
{% endfor %}
  ],
  "ExtensionSettings": {
{% for extension in brave_extensions %}
    "{{ extension.id }}": {
      "installation_mode": "force_installed",
      "update_url": "https://clients2.google.com/service/update2/crx"
    }{{ "," if not loop.last else "" }}
{% endfor %}
  }
}
