# Application bindings
$terminal = uwsm app -- $TERMINAL
$browser = omarchy-launch-browser

bindd = SUPER, RETURN, Terminal, exec, $terminal --working-directory="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER, slash, Passwords, exec, uwsm app -- bitwarden-desktop

# Simplified keybindings (SUPER + letter)
bindd = SUPER, A, Claude, exec, ~/.local/bin/omarchy-launch-or-focus-webapp Claude "https://claude.ai"
bindd = SUPER, B, Browser, exec, $browser
bindd = SUPER ALT, B, Browser (private), exec, $browser --private
bindd = SUPER, D, Docker, exec, $terminal -e lazydocker
bindd = SUPER, E, Thunderbird, exec, uwsm app -- env GDK_BACKEND=x11 thunderbird
bindd = SUPER, H, Homelab, exec, ~/.local/bin/omarchy-launch-or-focus-webapp Homelab "http://100.64.0.2:3000"
bindd = SUPER, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER, N, Editor, exec, omarchy-launch-editor
bindd = SUPER, R, Voice recording, exec, ~/.local/bin/whispering-toggle
bindd = SUPER, Y, YouTube, exec, ~/.local/bin/omarchy-launch-or-focus-webapp YouTube "https://youtube.com/"

# Keybindings that keep SHIFT (conflicts with window management)
bindd = SUPER SHIFT, C, VSCodium, exec, uwsm app -- codium --ozone-platform=wayland
bindd = SUPER SHIFT, F, File manager, exec, uwsm app -- nautilus --new-window
bindd = SUPER SHIFT, G, WhatsApp, exec, ~/.local/bin/omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus obsidian "uwsm app -- obsidian --disable-gpu --enable-wayland-ime"
bindd = SUPER SHIFT, T, Activity, exec, $terminal -e btop
