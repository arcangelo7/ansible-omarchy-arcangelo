---
- name: Omarchy system configuration
  hosts: localhost

  roles:
    - role: bloatware_removal
      tags: ['bloatware', 'cleanup']

    - role: applications_install
      tags: ['apps', 'install']

    - role: hyprland_config
      tags: ['hyprland', 'config']

    - role: system_settings
      tags: ['system', 'settings', 'monitor', 'keyboard']

    - role: applications_config
      tags: ['apps-config', 'config']
